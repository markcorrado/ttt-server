<%= form_for @truck do |f| %>

    <% if @truck.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@truck.errors.count, "error") %> prohibited
            this truck from being saved:
          </h2>
          <ul>
            <% @truck.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <p>
      <%= f.label :title %><br>
      <%= f.text_field :title %>
    </p>

    <p>
      <%= f.label :text %><br>
      <%= f.text_area :text %>
    </p>

    <p>
      <%= f.label :address %><br>
      <%= f.text_area :address %>
    </p>

    <p>
      <%= f.label :latitude %><br>
      <%= f.text_area :latitude %>
    </p>

    <p>
      <%= f.label :longitude %><br>
      <%= f.text_area :longitude %>
    </p>

    <p>
      <%= f.submit %>
    </p>
 <% end %>